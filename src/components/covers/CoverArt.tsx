import type{JournalEntry}from'@/types/journal';export function CoverArt({entry,src,large=false}:{entry:JournalEntry;src?:string;large?:boolean}){if(entry.locked&&entry.privacy==='private')return <div className="grid h-full min-h-64 place-items-center bg-gradient-to-br from-stone-800 to-stone-950 text-white"><div className="text-center"><div className="text-5xl">✦</div><p>Locked journal</p></div></div>;return <div className={`relative h-full min-h-64 overflow-hidden bg-gradient-to-br ${entry.coverLayout==='noir'?'from-stone-900 to-amber-950 text-white':entry.coverLayout==='botanical'?'from-emerald-100 to-amber-200':'from-amber-100 to-orange-300'}`}>{src?<img src={src} alt="Cover" className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center p-8 text-center"><div><p className="mb-3 text-xs uppercase tracking-[.4em]">{entry.entryDate}</p><h2 className={`${large?'text-6xl':'text-3xl'} font-serif`}>{entry.title}</h2>{entry.subtitle&&<p className="mt-3">{entry.subtitle}</p>}</div></div>}</div>}
