const allowed=new Set(["P","BR","H1","H2","H3","STRONG","B","EM","I","U","S","UL","OL","LI","BLOCKQUOTE","HR","A","IMG","AUDIO","SOURCE","FIGURE","FIGCAPTION","SPAN","DIV"]);
export function sanitizeHtml(input:string):string{ if(typeof window==="undefined") return input.replace(/<script[\s\S]*?<\/script>/gi,""); const doc=new DOMParser().parseFromString(`<main>${input}</main>`,"text/html"); doc.querySelectorAll("script,iframe,object,embed,style,link,meta").forEach(n=>n.remove()); const walk=(el:Element)=>{[...el.children].forEach(c=>{if(!allowed.has(c.tagName)){c.replaceWith(...Array.from(c.childNodes));return}[...c.attributes].forEach(a=>{const n=a.name.toLowerCase(),val=a.value;if(n.startsWith("on")) c.removeAttribute(a.name); if(["href","src"].includes(n)&&!/^(https?:|mailto:|blob:|data:image\/|data:audio\/|#)/i.test(val)) c.removeAttribute(a.name); if(!["href","src","alt","title","controls","class","data-asset-id","aria-label"].includes(n)) c.removeAttribute(a.name)}); walk(c)})}; walk(doc.body); return doc.body.querySelector("main")?.innerHTML??""}
export function excerptFromHtml(html:string,max=180):string{return sanitizeHtml(html).replace(/<[^>]+>/g," ").replace(/\s+/g," ").trim().slice(0,max)}
