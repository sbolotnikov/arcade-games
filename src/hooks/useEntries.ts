'use client';import{useEffect,useState}from'react';import type{JournalEntry}from'@/types/journal';import{entryRepository}from'@/db/repositories/journalRepository';export function useEntries(){const[entries,setEntries]=useState<JournalEntry[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const refresh=()=>entryRepository.list().then(setEntries).catch(e=>setError(String(e))).finally(()=>setLoading(false));useEffect(()=>{refresh()},[]);return{entries,loading,error,refresh}}
