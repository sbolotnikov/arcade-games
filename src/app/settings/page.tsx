import{BackupPanel}from'@/components/import-export/BackupPanel';export default function Page(){return <main className="mx-auto max-w-4xl space-y-6 p-8"><h1 className="font-serif text-5xl">Settings</h1><BackupPanel/><section className="rounded-[2rem] bg-white/75 p-6"><h2>Privacy and security boundaries</h2><p>Journal data is stored in this browser profile in IndexedDB. Clearing site data or using private browsing may remove it. Password locking encrypts entry content with AES-GCM and PBKDF2; the password, password hash, and derived key are not stored. Forgotten passwords cannot be recovered. This does not protect against malware, malicious extensions, screen capture, or an already-compromised device.</p><p>Create backups regularly. Standalone HTML exports are offline-readable; locked exports contain ciphertext and inline decryption code rather than plaintext content.</p></section><section className="rounded-[2rem] bg-white/75 p-6"><h2>Browser support</h2><p>IndexedDB, Web Crypto, Blob downloads, object URLs, and modern HTML media are required. MediaRecorder formats differ by browser, so the app feature-detects the best audio type.</p></section></main>}
